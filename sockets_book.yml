# ~/.tmuxinator/sockets_book.yml
# you can make as many tabs as you wish...

name: sockets book
root: ~/Dropbox/Dev/Ruby/working-with-tcp-sockets

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain bash
  - server: 
  - console: irb
